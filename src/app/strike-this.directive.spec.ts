import { StrikeThisDirective } from './strike-this.directive';

describe('StrikeThisDirective', () => {
  it('should create an instance', () => {
    const directive = new StrikeThisDirective();
    expect(directive).toBeTruthy();
  });
});
