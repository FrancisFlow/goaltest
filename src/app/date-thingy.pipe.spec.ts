import { DateThingyPipe } from './date-thingy.pipe';

describe('DateThingyPipe', () => {
  it('create an instance', () => {
    const pipe = new DateThingyPipe();
    expect(pipe).toBeTruthy();
  });
});
